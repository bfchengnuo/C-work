//本题是让求该数组的外元素之和，个人觉着有很多不足，，望大家优化
# include<stdio.h>
   int main()
{
	int a[3][4]={{3,8,9,10},{2,5,3,5},{7,0,-1,4}},s=0,i,j;
	for(i=0;i<3;i++)
	{
		for(j=0;j<4;j++)
		{
			if((i==1&&j==1)||(i==1&&j==2))  //舍去内侧的数组；不适合有多元素的数组
				s+=0;
			else
			    s+=a[i][j];
		}
	}
                printf("外侧元素之和为：%d\n",s);

	return 0;
}